# ------------------------------------------------------------------
# Supabase (required)
# Project Settings -> API in the Supabase dashboard
# ------------------------------------------------------------------
NEXT_PUBLIC_SUPABASE_URL=https://hxzqlhrwjzcbrvockwmc.supabase.co/rest/v1/
NEXT_PUBLIC_SUPABASE_ANON_KEY=sb_publishable_haZEWveZfL-KRjoaSyoXLg_Q3qWhiON

# Server-only. NEVER expose this in the browser and never prefix it
# with NEXT_PUBLIC_. Used by server actions for order creation and
# admin mutations.
SUPABASE_SERVICE_ROLE_KEY=YOUR-SERVICE-ROLE-KEY

# ------------------------------------------------------------------
# Site
# ------------------------------------------------------------------
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# ------------------------------------------------------------------
# Stripe (optional, future). Leave empty to run in
# "Pre-order / Pay at Pickup" mode. When both keys are present AND
# payment_stripe_enabled is turned on in admin Settings, the online
# payment option becomes available at checkout.
# ------------------------------------------------------------------
STRIPE_SECRET_KEY=
NEXT_PUBLIC_STRIPE_PUBLISHABLE_KEY=
STRIPE_WEBHOOK_SECRET=
